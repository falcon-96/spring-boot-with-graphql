type Query {
    animals: [Animal]
    animalById(id: ID!): Animal
}

type Mutation {
    admitAnimal(creature: Creature): Animal
}

input Creature{
    id: ID!
    name: String!
}

type Animal {
    id: ID!
    name: String!
}
