type Query {
    animals: [Animal]
    animalById(id: ID!): Animal
}

type Mutation {
    admitAnimal(animal: Animals): Animal
}

input Animals{
    id: ID!
    name: String!
}

type Animal {
    id: ID!
    name: String!
}
